# IMAP configuration (optional for demo)
IMAP_HOST=
IMAP_PORT=993
IMAP_SECURE=true
IMAP_USER=
IMAP_PASSWORD=

# SMTP configuration (optional for demo)
SMTP_HOST=
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# OpenAI (optional, improves drafting & classification)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Agent behavior
AUTO_REPLY=false
BASIC_REPLY_CONFIDENCE_THRESHOLD=0.6
